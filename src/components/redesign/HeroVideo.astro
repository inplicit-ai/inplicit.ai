---
// Vollflaechiger Video-Hero. Bis zum ersten Scroll zeigt die Seite nur das
// weisse Logo (aus Nav.astro) und die Headline. Pillbar und Aktionen blenden
// sich erst per .scrolled ein, genau wie auf der alten Seite.
export interface Props {
  lang?: 'en' | 'de';
}
const { lang = 'en' } = Astro.props;
const isDE = lang === 'de';
---

<section class="rhero" id="hero">
  <div class="rhero__bg" aria-hidden="true">
    <video
      class="rhero__video"
      autoplay
      muted
      loop
      playsinline
      preload="auto"
      poster="/hero-people-poster.jpg"
    >
      <source src="/hero-people.mp4" type="video/mp4" />
    </video>
    <span class="rhero__scrim"></span>
  </div>

  <div class="rhero__inner container">
    <h1 class="rhero__headline">
      {isDE ? (
        <>
          <span class="rhero__line">Ihre Organisation.</span>
          <span class="rhero__line">In Stunden verstanden.</span>
        </>
      ) : (
        <>
          <span class="rhero__line">Your organisation.</span>
          <span class="rhero__line">Understood in hours.</span>
        </>
      )}
    </h1>

    <p class="rhero__sub">
      {isDE ? (
        <>
          Inplicit macht das Erfahrungswissen Ihrer Mitarbeitenden sichtbar.<br />
          Unsere KI führt Interviews, erkennt konkrete Lösungswege und zeigt
          Verbesserungspotenziale auf.
        </>
      ) : (
        <>
          Inplicit makes your people’s practical experience visible.<br />
          Our AI runs the interviews, spots concrete solutions and surfaces
          room for improvement.
        </>
      )}
    </p>
  </div>
</section>

<style>
  .rhero {
    position: relative;
    isolation: isolate;
    display: flex;
    /* Headline sitzt im unteren Drittel, damit das Gesicht im Video frei bleibt */
    align-items: flex-end;
    padding-bottom: clamp(5rem, 15vh, 10rem);
    height: 100svh;
    min-height: 620px;
    max-height: 940px;
    overflow: hidden;
  }

  .rhero__bg {
    position: absolute;
    inset: 0;
    z-index: -1;
    background: var(--color-canvas-dark, #0b0c10);
  }

  .rhero__video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* Zwei Verlaeufe: von unten abdunkeln, dazu links, damit die linksbuendige
     Headline auch ueber der hellen Warnweste steht. */
  .rhero__scrim {
    position: absolute;
    inset: 0;
    background:
      /* Kopf-Vignette: sonst verschwindet das weisse Logo, wenn das Video oben hell ist */
      linear-gradient(180deg, rgba(11, 12, 16, 0.52) 0%, rgba(11, 12, 16, 0) 18%),
      linear-gradient(90deg, rgba(11, 12, 16, 0.58) 0%, rgba(11, 12, 16, 0.22) 44%, rgba(11, 12, 16, 0) 68%),
      linear-gradient(0deg, rgba(11, 12, 16, 0.72) 0%, rgba(11, 12, 16, 0.2) 54%, rgba(11, 12, 16, 0.08) 100%);
  }

  .rhero__inner {
    position: relative;
    width: 100%;
  }

  .rhero__headline {
    display: flex;
    flex-direction: column;
    max-width: 16ch;
    color: #fff;
    font-size: clamp(2.5rem, 6.2vw, 5.4rem);
    font-weight: 400;
    line-height: 1.03;
    letter-spacing: -0.045em;
  }

  .rhero__sub {
    max-width: 62ch;
    text-wrap: pretty;
    margin-top: var(--space-6);
    font-size: clamp(1rem, 1.35vw, 1.25rem);
    line-height: 1.55;
    color: rgba(255, 255, 255, 0.82);
    text-shadow: 0 1px 12px rgba(11, 12, 16, 0.45);
  }

  .rhero__line,
  .rhero__sub {
    opacity: 0;
    transform: translateY(18px);
    animation: rhero-in 0.85s var(--ease-out) forwards;
  }
  .rhero__line:nth-child(2) { animation-delay: 0.09s; }
  .rhero__sub { animation-delay: 0.2s; }

  @keyframes rhero-in {
    to { opacity: 1; transform: none; }
  }

  @media (max-width: 767px) {
    .rhero { height: 88svh; min-height: 540px; }
    .rhero__headline { max-width: 100%; letter-spacing: -0.035em; }
  }

  @media (prefers-reduced-motion: reduce) {
    .rhero__line,
    .rhero__sub { animation: none; opacity: 1; transform: none; }
    .rhero__video { display: none; }
  }
</style>
